<h1 class="text-xl font-semibold mb-4">Edit Project</h1>

<%= render "form", project: @project %>

<div class="mt-4">
  <%= link_to "Back", project_path(@project), class: "text-blue-600 hover:underline" %>
</div>
