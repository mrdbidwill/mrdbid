<% content_for :title, "Editing project" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-2xl">Editing project</h1>

  <%= render "form", project: @project %>

  <%= link_to "Show this project", project_path(@project), class: "btn-primary" %>
  <%= link_to "Back to projects", projects_path, class: "btn-primary" %>
</div>
