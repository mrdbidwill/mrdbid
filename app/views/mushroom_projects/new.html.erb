<h1 class="text-xl font-semibold mb-4">New Mushroom Project</h1>

<%= render "form",
           mushroom_project: @mushroom_project,
           projects: (@projects || []),
           mushrooms: (@mushrooms || []) %>

<div class="mt-4">
  <%= link_to "Back", mushroom_projects_path, class: "text-blue-600 hover:underline" %>
</div>
