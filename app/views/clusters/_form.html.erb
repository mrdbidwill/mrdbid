<%= form_with(model: cluster, class: "contents") do |form| %>
  <% if cluster.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-2 py-1 font-medium rounded-md mt-3">
      <h2><%= pluralize(cluster.errors.count, "error") %> prohibited this cluster from being saved:</h2>

      <ul class="list-disc ml-6">
        <% cluster.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :name %>
    <%= form.text_field :name, class: ["block shadow-sm rounded-md border px-2 py-1 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": cluster.errors[:name].none?, "border-red-400 focus:outline-red-600": cluster.errors[:name].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :description %>
    <%= form.textarea :description, rows: 4, class: ["block shadow-sm rounded-md border px-2 py-1 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": cluster.errors[:description].none?, "border-red-400 focus:outline-red-600": cluster.errors[:description].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :comments %>
    <%= form.textarea :comments, rows: 4, class: ["block shadow-sm rounded-md border px-2 py-1 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": cluster.errors[:comments].none?, "border-red-400 focus:outline-red-600": cluster.errors[:comments].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "btn-primary btn-responsive" %>
  </div>
<% end %>
