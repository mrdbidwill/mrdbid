<% content_for :title, "Showing storage location" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-2xl">Showing storage location</h1>
  <%= render @storage_location %>
  <%= link_to "Edit this storage_location", edit_admin_storage_location_path(@storage_location), class: "rounded-md px-2 py-1 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  <%= link_to "Back to storage locations", admin_storage_locations_path, class: "rounded-md px-2 py-1 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  <%# button_to "Delete this storage location", admin_storage_location_path(@storage_location), method: :delete, form_class: "sm:inline-block mt-2 sm:mt-0 sm:ml-2", class: "w-full rounded-md px-2 py-1 text-white bg-red-600 hover:bg-red-500 font-medium cursor-pointer", data: { turbo_confirm: "Are you sure?" } %>

</div>
