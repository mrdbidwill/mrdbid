<% content_for :title, "Editing camera make" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing camera make</h1>

  <%= render "form", camera_make: @camera_make %>

  <%= link_to "Show this camera make", admin_camera_make_path(@camera_make), class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  <%= link_to "Back to camera makes", admin_camera_makes_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
</div>
