<% content_for :title, "Fungus Types" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="py-1 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-2xl">Fungus Types</h1>
    <%= link_to "Back to Admin Dashboard", admin_root_path, class: "rounded-md px-2 py-1 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
    <%= link_to "New fungus type", new_admin_fungus_type_path, class: "rounded-md px-2 py-1 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  </div>

  <div id="fungus_types" class="mt-4 space-y-1">
    <% if @fungus_types.any? %>
      <% @fungus_types.each do |fungus_type| %>
        <div class="border rounded p-3 bg-white">
          <div class="flex flex-wrap items-center justify-between gap-2">
            <%= render fungus_type %>
            <div class="w-full sm:w-auto flex flex-col sm:flex-row space-x-2 space-y-1">
              <%= render "admin/shared/resource_actions", record: fungus_type, delete: false %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <p class="text-center my-10">No fungus types found.</p>
    <% end %>
  </div>
</div>
