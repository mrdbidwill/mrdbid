<% content_for :title, "Editing cluster" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing cluster</h1>

  <%= render "form", cluster: @cluster %>

  <%= link_to "Show this cluster", @cluster, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  <%= link_to "Back to species", species_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
</div>
