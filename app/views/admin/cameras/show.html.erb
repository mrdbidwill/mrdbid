<% content_for :title, "Showing camera" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Showing camera</h1>

  <%= render @camera %>

  <%= link_to "Edit this camera", edit_admin_camera_path(@camera), class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  <%= link_to "Back to cameras", admin_cameras_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-medium" %>
  <%#=
              <%# button_to "Delete this camera", admin_camera_path(@camera), method: :delete, form_class: "sm:inline-block mt-2 sm:mt-0 sm:ml-2", class: "w-full rounded-md px-3.5 py-2.5 text-white bg-red-600 hover:bg-red-500 font-medium cursor-pointer", data: { turbo_confirm: "Are you sure?" } %>
  %>
</div>
