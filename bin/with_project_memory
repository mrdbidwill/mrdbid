#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ "$#" -lt 1 ]]; then
  echo "Usage: bin/with_project_memory <command> [args...]"
  exit 1
fi

"$repo_root/script/ensure_project_memory_ack.sh"

exec "$@"
