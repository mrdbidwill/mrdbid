# user_roles.yml
admin_user_role:
  user: admin # Refers to "admin" in users.yml
  role: admin # Refers to "admin" in roles.yml

user_one_role:
  user: one # Refers to "one" in users.yml
  role: manager # Refers to "manager" in roles.yml
